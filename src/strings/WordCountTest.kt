package strings

import org.junit.jupiter.api.Assertions.*
import org.junit.jupiter.api.Test

internal class WordCountTest {
    @Test
    fun wordCount() {
        println (WordCount.wordCount("long long long way to to my home"))
    }
}